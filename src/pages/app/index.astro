---
import Layout from '../../layouts/Layout.astro'

export const prerender = false
---

<Layout>
  <main class="min-h-screen bg-slate-50">
    <header class="border-b border-slate-200 bg-white/80 backdrop-blur">
      <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <div class="space-y-1">
          <p class="text-sm uppercase tracking-wide text-emerald-500">Mon Budget</p>
          <h1 class="text-2xl font-semibold text-slate-900">Espace personnel</h1>
          <p id="current-user" class="text-sm text-slate-600">Chargement…</p>
        </div>
        <nav class="flex items-center gap-3 text-sm font-medium text-slate-600">
          <a href="/app/dashboard" class="rounded-lg px-3 py-2 transition hover:bg-emerald-50 hover:text-emerald-600">Dashboard</a>
          <a href="/app/import" class="rounded-lg px-3 py-2 transition hover:bg-emerald-50 hover:text-emerald-600">Importer</a>
          <a href="/logout" class="rounded-lg px-3 py-2 text-rose-500 transition hover:bg-rose-50 hover:text-rose-600">Déconnexion</a>
        </nav>
      </div>
    </header>

    <section class="mx-auto flex max-w-6xl flex-col gap-6 px-6 py-12">
      <div class="rounded-2xl border border-dashed border-slate-300 bg-white/70 p-6 text-center text-slate-600">
        <h2 class="text-lg font-medium text-slate-900">Bienvenue dans ton espace</h2>
        <p class="mt-2 text-sm">
          Cette page servira de base pour organiser la navigation vers les différentes fonctionnalités (Dashboard, Import CSV, Règles…).
        </p>
      </div>

      <div class="grid gap-4 sm:grid-cols-2">
        <article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
          <h3 class="text-base font-semibold text-slate-900">Prochaine étape</h3>
          <p class="mt-2 text-sm text-slate-600">
            Implémenter la page Dashboard avec les métriques de budget mensuel et les graphiques.
          </p>
        </article>
        <article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
          <h3 class="text-base font-semibold text-slate-900">Import de transactions</h3>
          <p class="mt-2 text-sm text-slate-600">
            Ajoute la page d’import CSV pour catégoriser tes dépenses et revenus automatiquement.
          </p>
        </article>
      </div>
    </section>
  </main>
</Layout>

<script type="module">
  import { supabase } from '/src/lib/supabaseClient'

  const userLabel = document.querySelector('#current-user')

  try {
    const {
      data: { user },
      error,
    } = await supabase.auth.getUser()

    if (error) throw error

    if (user?.email) {
      userLabel.textContent = `Connecté en tant que ${user.email}`
    } else {
      userLabel.textContent = 'Utilisateur non identifié'
    }
  } catch (error) {
    console.error('[app/index] getUser failed', error)
    userLabel.textContent = 'Utilisateur non identifié'
  }
</script>
